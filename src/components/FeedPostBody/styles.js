import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  // Body
  description: {
    paddingHorizontal: 10,
    lineHeight: 20,
    letterSpacing: 0.3
  },
  image: {
    width: '100%',
    aspectRatio: 1,
    marginTop: 10, 
  },
})