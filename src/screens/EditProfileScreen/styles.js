import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  rootContainer: {
    flex: 1,
  },
  infoContainer: {
    width: '100%',
    height: 'auto',
    padding: 10,
  },
  updateBtnText: {
    fontSize: 20
  }
});